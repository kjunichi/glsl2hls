<source>
  type forward
  port 24224
  bind 0.0.0.0
</source>
#<match **>
#  type stdout
#</match>
<match **>
  type copy
  <store>
    type pgjson
    host localhost
    port 5432
    database fluentd
    table fluentd
    user junichi
#  password  ""
    time_col time
    tag_col tag
    record_col record
  </store>
  <store>
    type stdout
  </store>
</match>
